import config from '../config';
const initialState = config.months;
const months = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default months;

